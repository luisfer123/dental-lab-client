import { PhoneTypePipe } from './phone-type.pipe';

describe('PhoneTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneTypePipe();
    expect(pipe).toBeTruthy();
  });
});
